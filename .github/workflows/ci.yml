name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.13

      - name: Install dependencies
        run: bun install

      - name: Build
        run: bun run build

  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.13

      - name: Install dependencies
        run: bun install

      # Typecheck the packages that are green today: @codecast/web and
      # @codecast/convex. The cli package's typecheck currently surfaces ~22
      # pre-existing errors tracked under item A4 — wiring it here would make
      # this gate red on day one for debt that is out of scope. Add
      # @codecast/cli to this filter once A4 clears its type errors; the
      # deploy.sh pre-deploy gate already hard-gates cli's own typecheck.
      - name: Typecheck (web + convex)
        run: bun run typecheck --filter=@codecast/web --filter=@codecast/convex

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.13

      - name: Install dependencies
        run: bun install

      - name: Lint (eslint flat config)
        run: bun run lint

  test-convex:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.13

      - name: Install dependencies
        run: bun install

      - name: Unit tests (convex package)
        working-directory: packages/convex
        run: bun test convex/

  test-web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.13

      - name: Install dependencies
        run: bun install

      - name: Unit tests (web package)
        working-directory: packages/web
        run: bun test

      - name: Unit tests (shared/tasks)
        working-directory: packages/shared
        # Scoped to the tasks suite: the package has other pre-existing test
        # files with unrelated failures that are not this job's concern.
        run: bun test tasks/

      - name: Unit tests (shared/contracts — capability layer)
        working-directory: packages/shared
        # The contracts are read by the CLI, the daemon, Convex and the browser,
        # so a regression here is a regression everywhere. The whole directory:
        # the capability layer, and the shared inbox projection with its golden
        # hash gate (tied to INBOX_PROJECTION_VERSION), digest vector and
        # property suite — the validation plan in
        # docs/architecture/sync-convergence.md rests on these running on every
        # merge.
        run: bun test contracts/

  test-cli:
    runs-on: ubuntu-latest
    steps:
      # fetch-depth 2 is what the injection-path filter below needs to diff a
      # push; a pull_request fetches its base branch itself.
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.13

      - name: Install tmux (e2e tests need a real tmux server)
        run: sudo apt-get update && sudo apt-get install -y tmux

      - name: Install dependencies
        run: bun install

      - name: Unit tests (cli package)
        working-directory: packages/cli
        # Exclude the e2e suite — it has its own step with --rerun-each for
        # resilience against tmux-resource flake under CI runner load.
        # DAEMON_BUILD_ID_CHECK turns on the daemon build id stamp check, which
        # walks the whole daemon import closure. Off locally so the normal test
        # loop stays fast.
        env:
          DAEMON_BUILD_ID_CHECK: "1"
        run: bun test src/ --bail=5

      - name: E2E messaging pipeline (real tmux + fake-claude shim)
        working-directory: packages/cli
        # Up to 3 full-suite attempts to absorb tmux-resource flake from
        # running 10 fresh sessions back-to-back on a contended runner.
        # Real regressions fail all 3 attempts; transient flake is filtered.
        # If you see this retry step gating frequently, treat that as a
        # signal to harden the test isolation, not as the desired norm.
        run: |
          set -e
          for attempt in 1 2 3; do
            echo "::group::E2E attempt $attempt"
            if bun test src/messaging.e2e.test.ts --timeout=60000; then
              echo "::endgroup::"
              echo "E2E passed on attempt $attempt"
              exit 0
            fi
            echo "::endgroup::"
            echo "::warning::E2E attempt $attempt failed; retrying"
            tmux kill-server 2>/dev/null || true
            sleep 2
          done
          echo "::error::E2E failed all 3 attempts — likely a real regression"
          exit 1

      # The injection path is the most fragile code in the daemon, so a change
      # to it runs the rest of the cold-boot matrix (ct-49536): the per-client
      # stale-draft cases against real client TUIs, plus the scripted pane
      # suites that need no binary at all. The matrix's cold-boot, mid-turn and
      # resume cells live in messaging.e2e.test.ts and already ran in the step
      # above. Every real-client cell self-skips when the binary is absent, so
      # this stays green on a runner with no agents and is meaningful on one
      # that has them.
      - name: Detect injection-path changes
        id: injection
        # The two-dot diff against the base tip is deliberately generous: a
        # branch behind main reports main's files as changed too. Over-running
        # the matrix costs a few minutes; missing an injection change costs a
        # regression nobody caught.
        run: |
          set -e
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            git fetch --no-tags --depth=1 origin "${{ github.base_ref }}"
            changed=$(git diff --name-only FETCH_HEAD HEAD || true)
          else
            changed=$(git diff --name-only HEAD~1 HEAD || true)
          fi
          if echo "$changed" | grep -qE '^(packages/cli/src/(daemon\.ts|tmuxPaste\.ts)|packages/shared/contracts/agentClients\.ts)$'; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi
          echo "changed injection paths: $(echo "$changed" | grep -cE '^(packages/cli/src/(daemon\.ts|tmuxPaste\.ts)|packages/shared/contracts/agentClients\.ts)$' || true)"

      - name: Cold-boot injection matrix (real clients where installed)
        if: steps.injection.outputs.run == 'true'
        working-directory: packages/cli
        # Same three-attempt shape as the E2E step above, and for the same
        # reason: spawning agent TUIs back-to-back on a contended runner is
        # resource flake, a real injection regression fails all three.
        run: |
          set -e
          for attempt in 1 2 3; do
            echo "::group::Injection matrix attempt $attempt"
            if bun test src/daemon.inject-clear.test.ts src/daemon.inject-enter-gate.test.ts src/daemon.inject-submit-verify.test.ts --timeout=240000; then
              echo "::endgroup::"
              echo "Injection matrix passed on attempt $attempt"
              exit 0
            fi
            echo "::endgroup::"
            echo "::warning::Injection matrix attempt $attempt failed; retrying"
            tmux kill-server 2>/dev/null || true
            sleep 2
          done
          echo "::error::Injection matrix failed all 3 attempts — likely a real regression"
          exit 1
