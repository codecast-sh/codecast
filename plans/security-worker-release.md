Security remediation plan pl-734; parent session jx7f70q owns integration. Read plans/security-execution.md and your numbered plan file plus the matching section of plans/security-review-*.md. Work in the shared main checkout (repo rule), never a worktree. Scan git status/diff for your files first; other sessions have uncommitted work, never revert or restage it. Do not commit, push, deploy, install dependencies, or run scripts/vendor-platform.sh; tell the parent if you need any of those. Canonical shared code lives in ~/src/platform; never edit platform/packages by hand. Typecheck only with cast check. Run long commands in a named tmux session. Write a failing regression through the real handler first, then fix, then verify positive paths still work. No real accounts, real credentials, or load tests. Claim your task with cast task start, post milestones as task comments, and when verified, mark the task done with a summary. Send the parent one final result with cast send jx7f70q (heredoc stdin). Bring the parent only real product tradeoffs or regressions.

Task ct-53059, plan file plans/008-security-release-assurance.md (PARENT-07, PARENT-08, PARENT-09 and the coverage matrix). HUMAN PRIORITY above all: no disruption or regression for existing users. Every CLI and desktop build already in the field must keep updating without user action; new verification may only harden clients that ship it, and must never strand a client on an old version. Do step 1 (no shell interpolation in update.ts) and step 3 (ws upgrade, audit triage ledger) fully. For step 2 (signed manifest with a pinned key): design it and implement verification behind a path that accepts today's releases, but DO NOT create, store or rotate any signing key or repo secret; bring the parent the key custody question as a product decision with the exact rollout order. Step 4 (entrypoint inventory and permission matrix): generate the machine readable inventory and add negative handler tests for the highest risk unresolved cells; list the rest as named open cells. Step 5 live infra checks are read only; mark missing access explicitly. Do not run cut-cli-release, release dry runs with publishing side effects, or builds that upload anything.
