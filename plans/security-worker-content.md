The human now authorizes full end-to-end completion of security plan pl-734. Parent/root is jx7f70q. Read /Users/ashot/src/codecast/plans/security-execution.md and your numbered plan. Earlier proposal-only instructions are superseded by the human's latest request. You implement and test; parent integrates/releases. Bring concrete product tradeoffs/regressions to parent; don't stop for routine implementation decisions.

Work in the shared main checkout, NOT a worktree (repo requirement overrides the agent definition). Scope ownership is strict. Capture/read existing diffs before editing and preserve all unrelated work. Do NOT commit/stage/push/deploy, install/update dependencies, restart shared services, run full suites, or run scripts/vendor-platform.sh. Never hand-edit platform/packages. Long commands must run in a named tmux session; typecheck only cast check. Use Bun focused tests. No real-account attack, credential output, load attack or real OS media/clipboard. Use cast browser in human Chrome for safe local fixtures; no separate browser. If browser extension unavailable, report it and continue nonbrowser work.

Claim your parent task using cast task start; update milestones in that task. Return bounded slices as soon as urgent fixes are tested so parent can arrange review; continue other owned cases unless awaiting a product decision. Keep task in_review/in_progress, not done: parent closes after independent review/e2e. Report your session ID, files, exact gates/logs, negative and positive cases, and unresolved verification or product decisions. Send one material result to parent via cast send jx7f70q with heredoc stdin. Do not edit the parent's index/ledger. Do not spawn additional workers; concurrency belongs to parent.

Task ct-53054. Read plans/003-security-content-desktop.md and CLIENT-01/02/03/05/06/PARENT-10. OWN castChart.ts, canvasSanitize.ts, HtmlSnippet.tsx, related rendering tests, mobile CastCanvas.tsx/tests, Electron main.js/preload.js/policy tests, FrameBackend capability delegation, web server response policy. EXCLUDE publishing/artifact controls and auth/cache/store. Existing Electron main.js diff adds plain breakout app routing near 694 and appWindow.main.test.js has a corresponding test; preserve both. Root captured them. Other web diff owners are unrelated component flows: never stage/reformat whole files beyond your scope.

Deliver chart hydration URL/output defense FIRST with real pipeline tests (per-row channels, unsafe SVG URLs, no forbidden image fetch, ordinary links/charts). Prove harmless execution marker no longer runs through actual sanitizer+hydrate in human Chrome local fixture. Continue canvas no-egress and trusted outer layout containment while preserving supported widgets/charts/fullscreen. If broad CSS/media restrictions change supported behavior, send concrete alternatives to parent before choosing destructive restrictions.

Then centralize Electron main-frame/sender and exact-origin policy across privileged IPC, navigation and request-frame permission checks. A fake event test isn't proof arbitrary iframe IPC exists; packaged test with fake capabilities is required. Preserve registered shell popouts, dev origin, call host, OAuth. Do not access actual clipboard/media or launch a second Chrome. Existing Electron harness tests call some handlers with null sender; update harness realism, don't keep null allowed to satisfy tests. Introduce web headers report-only where enforce mode requires product/compatibility review; parent decides based on real violation evidence. Return slices with exact tests and remaining packaged/mobile validation.

