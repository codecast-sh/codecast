Review the bounded identity security implementation for ct-53052 under plan pl-734. Parent session jx7f70q. Read plans/001-security-identity.md, plans/security-review-backend.md and task context. Stay read-only on product code; synthetic regression scripts/logs under /tmp are allowed. Do not claim or close task, install, vendor, commit, deploy or test real accounts. User authorizes full remediation; product decisions remain pending, so verdict must separate code correctness from unfinished rollout/product work.

Canonical implementation (uncommitted) in /Users/ashot/src/platform/packages/auth/src/convex/{providers.ts,callbacks.ts,createAuthConfig.ts,createAuthConfig.test.ts,identity.security.test.ts}; use git diff there. Codecast implementation in packages/convex/convex/{users.ts,slackSync.ts,slackSync.test.ts,users.aliasVerification.test.ts}; inspect scoped diff. Other changes belong to other sessions. Source is not vendored yet. Canonical worker jx70xjq reports 97 passing package tests; Slack55; typechecks pass. Logs /tmp/ct-53052.

Be adversarial. Trace real @convex-dev/auth store/createAccount/provider linking, returning subjects, simultaneous first signup/OTP, recovery, absent or ambiguous proof and OAuth API errors. Verify old accounts cannot be adopted based on user email alone, and proof cannot be injected through password input. Inspect subject login updates and safe legacy recovery. Check callback/profile verified fields, email normalization and redirect canonicalization, exact-origin/native schemes. Slack proofs must bind workspace+subject to the authenticated user; aliases and admin/manual mappings need distinct policy. Trace automatic profile refresh so a failed proof cannot leave unauthorized DM visibility unnoticed. Verify regressions test the actual library path, not replacement mocks.

Root confirmed production AUTH_EMAIL_VERIFICATION absent, RESEND_API_KEY present, values not logged. Keeping immediate unique signup is pending a human policy choice. Alternate aliases are contact-only after this fix; OTP aliases vs SlackOAuth-only pending. Existing historical mappings not migrated/revoked. Bring concrete introduced regressions and residual vulnerabilities, not vague hardening suggestions. Historical authority audit is root-owned and remains open.

Run focused tests only if useful; long commands in tmux; typecheck only cast check. No browser/production needed for this read-only review. Write plans/security-review-identity-implementation.md with PASS/NEEDS_CHANGES for bounded code plus remaining acceptance items, exact citations, reproduction and suggested fix. Post task review comment and send one final receipt via cast send jx7f70q. Do not silently edit implementation.
