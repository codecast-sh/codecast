Security remediation plan pl-734; parent session jx7f70q owns integration. Read plans/security-execution.md and your numbered plan file plus the matching section of plans/security-review-*.md. Work in the shared main checkout (repo rule), never a worktree. Scan git status/diff for your files first; other sessions have uncommitted work, never revert or restage it. Do not commit, push, deploy, install dependencies, or run scripts/vendor-platform.sh; tell the parent if you need any of those. Canonical shared code lives in ~/src/platform; never edit platform/packages by hand. Typecheck only with cast check. Run long commands in a named tmux session. Write a failing regression through the real handler first, then fix, then verify positive paths still work. No real accounts, real credentials, or load tests. Claim your task with cast task start, post milestones as task comments, and when verified, mark the task done with a summary. Send the parent one final result with cast send jx7f70q (heredoc stdin). Bring the parent only real product tradeoffs or regressions.

Task ct-53053 follow-up (BACKEND-09 device bound tokens). State: shipped in CLI 1.1.154 and Convex, with REQUEST_DEVICE_BOUND_TOKENS=false in packages/cli/src/bearerToken.ts. A bound token (secret prefix bound_) is presented as <secret>.<device_id> (platform/packages/auth/src/tokenFormat.ts) and verified in verifyApiToken (canonical ~/src/platform/packages/auth/src/convex/apiTokens.ts). apiTokens.mintForDevice lets a BOUND caller token, presented with its own device, mint a token bound to another device id. Host provisioning (pushCodecastConfig in packages/cli/src/browser/provisionLinux.ts) already uses it. Device ids come from packages/cli/src/remote/device.ts (a hash of ~/.codecast/.machine_key, pinned to hardware by .device_binding.json). packages/cli/src/machineKey.ts rotates the key on new hardware and keeps .machine_key.prev.

HUMAN PRIORITY: no disruption for existing users. Before the flag can turn on, these must not break:
1. Migration Assistant, a disk clone, or a host landing on new hardware changes the device id, and a bound token is then refused, so the user would have to run cast auth again. Make the CLI heal itself. When the stored token is bound and the current device id is not the one it was minted for, present it once with the minted device id to mintForDevice for the NEW device id. Store the minted device id in config beside the token at mint time; fall back to deriving it from .machine_key.prev. Store the new token. Do this in the one place every authenticated CLI path goes through (bearerFromStored or the config load), guarded so it runs once per change, never loops, and is safe when several processes (daemon plus a cast command) hit it at once. This gives up no protection: a whole ~/.codecast copy was never something binding could stop.
2. packages/cli/scripts/mac-full-setup.sh:36-42 copies the decrypted laptop secret to the Scaleway Mac. Route it through pushCodecastConfig, or mint through mintForDevice with the Mac's device id (cast remote hosts).
3. packages/cli/docs/remote-sessions.md:212 tells people to copy the decrypted token by hand. Replace it with the provisioning command, or cast auth on the Mac.
Tests through the real functions, red first: a migrated machine (key rotated, prev kept) heals exactly once and keeps working; two concurrent processes heal once; an unbound token is untouched; a bound token with no recoverable minted id fails with a clear "run cast auth" message. Keep REQUEST_DEVICE_BOUND_TOKENS false; the parent flips it after review. Canonical auth changes go in ~/src/platform, and tell the parent when to vendor.
