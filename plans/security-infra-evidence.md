# Release and infrastructure evidence (plan 008, step 5)

Read-only checks run 2026-09-24 00:21 UTC from the shared main checkout at f055a9c71, bun 1.3.14, gh signed in as ashot (scopes repo, workflow, read:org), railway CLI 5.26.1 signed in, aws CLI profiles default/union/footage. No secret value was printed; names and metadata only. "Missing access" is recorded as such and never as passed.

| # | check | result | finding | owner |
|---|---|---|---|---|
| 1 | Release build path | verified by reading `.github/workflows/cut-cli-release.yml` and `finalize-cli-release.yml` | Binaries build on a macOS runner from `ref: main`, darwin binaries are signed with the Developer ID (team WRG9THCK9Q) and `codesign --verify --strict` runs in CI; finalize hashes staged objects, publishes immutable objects under `cli/releases/<version>/<sha>/`, verifies the public bytes, then writes `latest.json` last. A new optional step signs `latest.json` when `RELEASE_MANIFEST_SIGNING_KEY` and `RELEASE_MANIFEST_KEY_ID` exist (they do not yet). | release owner jx7f70q |
| 2 | Repository secrets | `gh secret list` (names, dates) | 12 secrets: CONVEX_ADMIN_KEY, CONVEX_URL (2026-03-02), CWS_* (Chrome Web Store, 2026-09), MACOS_SIGN_CERT_P12 + PASSPHRASE (2026-08-27), R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY (2026-03-02), R2_ENDPOINT. **The production Convex admin key lives in CI secrets for a retired backup workflow that is `workflow_dispatch` only.** Rotation of the R2 keys and the admin key has not happened since March; treat as a rotation candidate under ct-39959. No signing key for the manifest exists. | founder (custody), release owner |
| 3 | Branch protection on `main` | `gh api repos/codecast-sh/codecast/branches/main/protection` = 404 "Branch not protected"; `rulesets` = [] | **`main` has no protection and no rulesets.** Every release workflow checks out `main`, so any collaborator push (or a compromised collaborator token) becomes a signed release. Recommend a ruleset: require PR or signed commits, block force push and deletion, restrict who can push. | founder |
| 4 | Actions policy | `actions/permissions`: enabled, `allowed_actions: all`, `sha_pinning_required: false`; `permissions/workflow`: default read, cannot approve PRs | Workflow token defaults are least privilege (good); third party actions are unrestricted and not SHA pinned. `cut-cli-release` grants `actions: write`, `finalize` and `cut-extension-release` grant `contents: write` (needed for tags/releases). Recommend pinning `oven-sh/setup-bun`, `actions/checkout` to SHAs and restricting allowed actions to verified creators. | release owner |
| 5 | Laptop release path | read `packages/cli/scripts/build-binaries.sh`, `deploy.sh` | Signs with the same identity; `CODECAST_SKIP_SIGN=1` ships ad-hoc binaries with a warning. After this change a signed client refuses an ad-hoc darwin release, so that escape hatch no longer reaches signed clients (documented in release-signing.md). `deploy.sh` publishes `latest.json` unsigned; acceptable until signing becomes required. | release owner |
| 6 | Update client controls | code + tests (this task) | Manifest entry validation, bounded download, argv curl, darwin signature gate, installer digest check, signed manifest path with no key pinned. Receipts in the ct-53059 task comments. | this task |
| 7 | R2 bucket IAM / token scopes | **missing access**: the R2 endpoint and the token's permission set are not readable from this machine (no R2 endpoint in the environment; Cloudflare token scopes are only visible in the Cloudflare dashboard) | Cannot confirm whether the CI R2 token is scoped to the `codecast` bucket or account wide, nor whether it can delete objects (the finalize flow relies on immutable objects). Founder to read the token in the Cloudflare dashboard and record scope here. | founder |
| 8 | Network exposure | read only: Convex http.ts routes (121 registered), dl.codecast.sh served by Cloudflare (HTTP/2, `cache-control: no-cache` on `latest.json`) | Public surface is the Convex HTTP router plus Railway web plus R2 through Cloudflare. HTTP action bodies are read with `request.json()` without an explicit byte cap; the platform cap (Convex HTTP action request limit) is the only bound. Open cell: explicit body bound on `/cli/exchange-token`, `/cli/claim-auth` and the OAuth callbacks. | backend worker ct-53053 |
| 9 | Backups | read `infra/pg-backup/backup.sh`, `Dockerfile`, `railway.json`; `railway list` shows the `codecast` project; GitHub `backup.yml` is retired (dispatch only) | Full `pg_dump -Fc` streamed to R2 from inside Railway; a failed dump removes the partial object. **Restoration has not been exercised in this task**: restoring into an isolated environment needs a synthetic dump and a scratch Postgres, and the R2 listing needs the endpoint (missing access, row 7). Recommend a quarterly restore drill with a synthetic database, recorded here. | founder / release owner |
| 10 | Secret history scan | bounded: `git log -p -n 400` grepped for AWS, Anthropic, GitHub, Slack, Google, Stripe, Resend key shapes and PEM private key headers, test/fixture lines excluded | 0 matches in the last 400 commits. Not a full history scan: gitleaks and trufflehog are not installed here; a full scan over all refs is an open item (run `gitleaks detect --source . --redact` on a machine with the tool). | release owner |
| 11 | Telemetry redaction | grep of `packages/web/lib/analytics.ts` for beforeSend/redact/mask/denylist | No redaction hook or property denylist found in the analytics wrapper. Open cell: enumerate the properties sent to PostHog (and Sentry, if enabled) and add a denylist for message text, paths and tokens; session recording flags must be confirmed off. | content worker ct-53054 / release owner |
| 12 | Storage capability revocation and quota | read `packages/convex/convex/images.ts` generateUploadUrl and `http.ts` `/cli/vault/upload-url`, `/cli/images/upload-url` | Upload URLs require an authenticated user or api_token; no per user quota or rate limit on upload URL minting is visible, and a storage id once issued is never revoked. Open cell: quota on `generateUploadUrl` per user per hour; revocation on account removal. | backend worker ct-53053 |
| 13 | User supplied GitHub identity downstream | grep of `github_username` comparisons | `comments.ts:495` resolves @mentions by `github_username` or display `name` (spoofable by a user who sets a matching name); `prCli.ts:308` filters "mine" by author handle (display only); `prShepherd.ts:481` attributes PR comments to the author by handle. The mention resolution is the one that grants something (a notification to a chosen user); open cell for the identity worker: resolve mentions by verified account link, never by display name. | identity worker ct-53052 |
| 14 | Linux browser sandbox / credential separation | grep of `packages/cli/src/browser` | Agent launched Chrome uses a per agent `--user-data-dir`; no `--no-sandbox` flag in the tree. Plan 004 owns the deeper isolation question (LOCAL-*). | local worker ct-53055 |
| 15 | Config path symlinks | read `packages/cli/src/config/configDir.ts` | `CODECAST_DIR` from the environment or `~/.codecast`, no `realpath` or symlink refusal: a symlink planted at `~/.codecast` by another local user or process redirects config, token and state writes. Open cell for plan 004: refuse a symlinked config dir (lstat) or resolve and pin it on first use. | local worker ct-53055 |
| 16 | Provider key rotation | not performed (read only task) | Reuse ct-39959 for the ECDH provider key plan; add the CI R2 keys and the Convex admin key (row 2) to that rotation list. | founder |
| 17 | Deployment versions in the field | `https://dl.codecast.sh/latest.json`: CLI 1.1.153 (source 76d3f5c4); desktop 1.1.116 (git log); Convex prod deploys tracked by the parent; web ships from Railway on push to main | The release owner records backend, web, CLI, desktop and mobile versions at release time; this task's changes reach the field only through the next CLI release (client side) and the next `finalize` run (workflow side). Old clients keep updating because nothing in the manifest changed shape. | release owner jx7f70q |

## Compatibility matrix for this change

| surface | change | oldest client still served | note |
|---|---|---|---|
| latest.json | unchanged shape; optional `size` per entry accepted by new clients, ignored by old | every client | `latest.json.sig` appears only once a key exists; old clients never request it |
| CLI updater | validation, bounded download, argv curl, darwin gate | n/a (client side) | a client older than this change keeps its current behaviour until it updates through it |
| install.sh | manifest + digest | n/a (fetched fresh from web) | fails closed when the manifest is unreachable, same host as the binary |
| bridge ws | 8.21.3 | n/a | loopback server; extension protocol unchanged |
