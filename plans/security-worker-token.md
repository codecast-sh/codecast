Security remediation plan pl-734; parent session jx7f70q owns integration. Read plans/security-execution.md and your numbered plan file plus the matching section of plans/security-review-*.md. Work in the shared main checkout (repo rule), never a worktree. Scan git status/diff for your files first; other sessions have uncommitted work, never revert or restage it. Do not commit, push, deploy, install dependencies, or run scripts/vendor-platform.sh; tell the parent if you need any of those. Canonical shared code lives in ~/src/platform; never edit platform/packages by hand. Typecheck only with cast check. Run long commands in a named tmux session. Write a failing regression through the real handler first, then fix, then verify positive paths still work. No real accounts, real credentials, or load tests. Claim your task with cast task start, post milestones as task comments, and when verified, mark the task done with a summary. Send the parent one final result with cast send jx7f70q (heredoc stdin). Bring the parent only real product tradeoffs or regressions.

Task ct-53053, remaining finding BACKEND-09 only (the other rows shipped). Canonical code: ~/src/platform/packages/auth/src/convex/apiTokens.ts; Codecast: packages/convex/convex/http.ts cliRoute and direct callers. FACTS: production has 285 API tokens and zero carry device_id; nothing mints a bound token today. The binding is enforced only in cliRoute (http.ts ~3905), which then strips device_id before calling handlers, so verifyApiToken deliberately accepts bound tokens (pinned by apiTokens.deviceBinding.test.ts). Direct Convex calls to the ~541 public functions taking api_token never pass the gate. HUMAN DECISION: no token rotation, no grace window, no disruption. REQUIRED ORDER: (1) close the direct path for bound tokens so a bound token is accepted only when its device is proven, while the HTTP path from the right machine keeps working (a cliRoute verified request must still authenticate its handler; design how the gate result reaches the handler without widening closed validators or trusting client fields); (2) make the CLI send device_id on direct calls; (3) only after that, bind newly minted tokens. Existing unbound tokens keep working and are not rotated. Setup tokens are already refused as bearers by the parent (done, do not redo). Update the pinned tests to the new design with real request path coverage for both doors. Canonical edits go in ~/src/platform; parent vendors.
