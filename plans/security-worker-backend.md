The human now authorizes full end-to-end completion of security plan pl-734. Parent/root is jx7f70q. Read /Users/ashot/src/codecast/plans/security-execution.md and your numbered plan. Earlier proposal-only instructions are superseded by the human's latest request. You implement and test; parent integrates/releases. Bring concrete product tradeoffs/regressions to parent; don't stop for routine implementation decisions.

Work in the shared main checkout, NOT a worktree (repo requirement overrides the agent definition). Scope ownership is strict. Capture/read existing diffs before editing and preserve all unrelated work. Do NOT commit/stage/push/deploy, install/update dependencies, restart shared services, run full suites, or run scripts/vendor-platform.sh. Never hand-edit platform/packages. Long commands must run in a named tmux session; typecheck only cast check. Use Bun focused tests. No real-account attack, credential output, load attack or real OS media/clipboard. Use cast browser in human Chrome for safe local fixtures; no separate browser. If browser extension unavailable, report it and continue nonbrowser work.

Claim your parent task using cast task start; update milestones in that task. Return bounded slices as soon as urgent fixes are tested so parent can arrange review; continue other owned cases unless awaiting a product decision. Keep task in_review/in_progress, not done: parent closes after independent review/e2e. Report your session ID, files, exact gates/logs, negative and positive cases, and unresolved verification or product decisions. Send one material result to parent via cast send jx7f70q with heredoc stdin. Do not edit the parent's index/ledger. Do not spawn additional workers; concurrency belongs to parent.

Task ct-53053. Read plans/002-security-backend-authority.md and BACKEND-03/04/05/06/08/09. OWN dispatch.ts/tests, managedSessions.ts/tests, data.ts/workspace list authorization/tests, and findMessageByContentPublic ONLY in messages.ts plus test/caller updates for share-token requirement. Initially EXCLUDE users.ts, http.ts, canonical auth apiTokens.ts (later credential contract work), Slack and any unrelated session delivery logic. messages.ts has active image materialization edits by jx7cthg near lines 865/1416/1948; preserve them. Parent coordinated your endpoint edit near 2583 with that session. jx79ech owns daemon paste/delivery changes; do not edit daemon.ts/pendingMessages.ts without parent coordination.

First urgent slice: bind document IDs to actual table before generic patch; actual fake DB helper must model normalizeId correctly and add a disposable real Convex test if available. Audit field allowlists and don't break legitimate outbox patches. Add negative membership/command/token wrong-table cases plus positive ordinary updates. Second: authorize conversation execution before managed-session cleanup/register/relink, and again pending reads/acks; preserve explicit cross-user runner handover by understanding existing runner/session_owners logic. Require no deletion/side effects on denial. Do not substitute visibility/team membership for execution authority.

Then repair all-workspace privacy and public content token possession with bounded search, testing public callers not just helpers. Finish by inventorying direct token transport bypass and propose a backwards-compatible enforcement design; parent assigns canonical users/http scope after identity work. Return this as remaining work, not falsely completed parent task.

