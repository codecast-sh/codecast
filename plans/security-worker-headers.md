Security remediation plan pl-734; parent session jx7f70q owns integration. Read plans/security-execution.md and your numbered plan file plus the matching section of plans/security-review-*.md. Work in the shared main checkout (repo rule), never a worktree. Scan git status/diff for your files first; other sessions have uncommitted work, never revert or restage it. Do not commit, push, deploy, install dependencies, or run scripts/vendor-platform.sh; tell the parent if you need any of those. Canonical shared code lives in ~/src/platform; never edit platform/packages by hand. Typecheck only with cast check. Run long commands in a named tmux session. Write a failing regression through the real handler first, then fix, then verify positive paths still work. No real accounts, real credentials, or load tests. Claim your task with cast task start, post milestones as task comments, and when verified, mark the task done with a summary. Send the parent one final result with cast send jx7f70q (heredoc stdin). Bring the parent only real product tradeoffs or regressions.

Finding PARENT-10 (plan 003 step 5): main web responses lack an enforced CSP, frame-ancestors, HSTS and isolation headers. Headers are set in packages/web/server/index.ts. Live today (curl -I https://codecast.sh/): content-security-policy-report-only with script-src 'self'; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'self', but NO report-uri/report-to, so no violations are collected; no strict-transport-security.

HUMAN PRIORITY above all: no disruption or regression for existing users. Nothing may start blocking in a browser until real reports prove it is safe. Steps:
1. Add violation reporting to the report-only policy. Prefer Sentry's CSP security endpoint if the project already uses Sentry in web (check packages/web for the DSN and how it is configured); otherwise propose the cheapest sink and bring it to the parent before adding a Convex write path. Sample or rate bound it.
2. Add strict-transport-security with a max-age only (no includeSubDomains, no preload) after confirming every codecast.sh response is already served over HTTPS and http redirects.
3. Draft the enforced policy, but ship it only as report-only. List every legitimate source the app needs (PostHog, Sentry, fonts, Convex websocket, the desktop app shell, the Chrome extension, cast canvas iframes, published pages at a.codecast.sh, OAuth returns, Vite dev origins on localhost).
4. Header tests beside the server, and a real browser check with cast browser that auth, charts, calls, OAuth return and the desktop windows still work with the new headers on the local server.
Record in plans/security-closure-ledger.json under PARENT-10 what is live, what is report-only, and the date after which the parent should review reports and decide on enforcement.
