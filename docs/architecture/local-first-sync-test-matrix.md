# Local-First Sync — Adversarial Test Matrix

**Campaign:** exhaustive adversarial verification of the v2 local-first sync layer (pl-205, task ct-40155).
**Scope:** `packages/web/store/local-first/*`, `packages/web/hooks/{useLocalView,useConversationComments,useSyncBuckets}.ts`, Convex view/command substrate. The fenced execution rail is out of scope.
**Method:** every row gets (1) a steelman for why the issue exists, (2) a conclusive experiment, (3) a verdict. `FOUND → FIXED <commit>` or `ABSENT: <test + hand-review note>`. No row closes without a verdict.

Verdict key: **PENDING** (not yet run) · **FOUND** (reproduced) · **FIXED(commit)** · **ABSENT** (test passed + hand review) · **N/A** (unreachable in current rollout stage; noted for the step that makes it reachable).

---

## A. Engine / source & writer epochs / ordering

| id | dimension combo | potential issue | steelman | experiment | verdict | notes |
|---|---|---|---|---|---|---|
| ENG-01 | single tab × remount race × demand view | `CompleteViewSource.close()` calls `engine.invalidateSource(viewKey)` unconditionally, which can delete a **successor** session's live source entry for the same view key (rapid unmount/remount of the same conversation, StrictMode, or close-with-inflight-deliver). All later applies of the live session throw `StaleLocalFirstSourceError`, which `LocalViewSession.report` swallows as supersession → view silently stops applying server results until remount. | `invalidateSource` takes only the key; `close()` is queued behind in-flight delivers whose IDB rw txns serialize BEFORE the successor's `claimViewWriter`, so the successor's `sources.set` can land before the old session's queued delete runs. Nothing re-opens a deleted source. | `writerHandoff.test.ts` "closing a superseded source leaves the successor source applicable" — RED pre-fix (successor apply threw stale), GREEN post-fix. | **FOUND → FIXED** | Fix: `invalidateSource(viewKey, sourceEpoch?)` deletes only its own entry; all internal invalidations pass their epoch. Session-level heal (TAB-01 fix) additionally recovers any residual variant. |
| ENG-02 | single tab × stale callback | An older in-flight apply (captured earlier sourceSequence) commits after a newer one, resurrecting stale rows. | Async callbacks can complete out of order; only `sourceSequence` ordering protects. If capture-order ≠ commit-order the adapter check `sourceSequence <= writer.sourceSequence` must reject. | engine.test.ts "stale callbacks cannot win" + adapter source-sequence fence (stale sequence rejected inside the txn); randomized interleavings in `convergenceMatrix.test.ts`. | **ABSENT** (tests + hand review) | Ordering is enforced durably in the adapter, not just in memory. |
| ENG-03 | single tab × principal switch racing in-flight applies | Apply captured under principal epoch N commits after `invalidatePrincipal(N+1)` → cross-principal write. | `commit` re-checks token in `assertCurrent` and in the txn guard, but the guard runs inside the adapter txn — verify the guard actually aborts the durable txn. | `convergenceMatrix.test.ts` "an apply captured before a principal switch cannot commit after it" — rejected pre-transaction, head unchanged. In-txn re-check via commit guard (engine passes assertCurrent as the txn guard). | **ABSENT** (test + hand review) | |
| ENG-04 | single tab × duplicate delivery | Duplicate identical complete result (same revision, same content) must be idempotent, not an error and not a rewrite that breaks anything. | `compareCoverage`→equal + contentEqual→pass performs a full deleteView+rewrite; check no grant/writer state corruption and head advances harmlessly. | `convergenceMatrix.test.ts` "an exact duplicate complete result reapplies without error or corruption". | **ABSENT** (test) | |
| ENG-05 | single tab × commit lands but token stale post-commit | Durable commit succeeds, then post-txn `assertCurrent` throws → publish + broadcast skipped; same-tab UI and other tabs never notified of a real durable head advance. | `engine.commit` calls `assertCurrent(token)` after adapter.commit and before publish/notify/broadcast; supersession right after the txn drops the notification on the floor. Focus-reconcile recovers other tabs but the CURRENT tab's other views don't republish until the next commit. | `convergenceMatrix.test.ts` "a commit that goes stale post-transaction stays durable but unpublished" — durable commit retained, publish/broadcast skipped, reconcileDurableHead() recovers the notification. | **ABSENT-BY-DESIGN** (test) | Liveness-only window; focus/visibility reconcile (wired in PrincipalLocalStateProvider) is the recovery path. |
| ENG-06 | delta path (regression guard) | `applyDelta` cursor gap/duplicate handling wrong (unused by current slices but part of the boundary). | Cursor precondition uses `expectedCursor` equality; duplicate replay must be rejected as "did not advance" — but the contract says duplicate application should be **idempotent**, not an error thrown into a live source. | `commandMatrix.test.ts` "a replayed ordered delta is rejected at the cursor and changes nothing" — replay rejected loudly at the expectedCursor precondition, stream state uncorrupted, continuation from the committed cursor works. | **ABSENT** (test) | §9.3 "duplicate-safe replay" is satisfied at the no-corruption level: replay is a loud reject, not a silent no-op. A future delta source runtime must catch the reject and resume from the durable cursor — note for Track B5 (default: never built). |

## B. Multi-tab (shared origin, BroadcastChannel path)

| id | dimension combo | potential issue | steelman | experiment | verdict | notes |
|---|---|---|---|---|---|---|
| TAB-01 | two tabs × same view × writer supersession | When tab B claims the writer for a view, tab A's applies are permanently fenced out. If B closes (or goes to bfcache), **no tab ever re-claims** — A keeps its live Convex subscription but silently discards every result; the durable view freezes while the server moves on. | `LocalViewSession` has no re-claim/retry path: supersession errors are swallowed forever. The design doc (§12.3) requires writer *handoff*; the implementation only has claim-on-open. | Two adapters over one fake-IDB database, two engines. Session A opens, session B opens (steals writer). Deliver r1 to both (B applies). Close B. Deliver r2 to A only. `writerHandoff.test.ts` "a surviving tab re-claims the writer…" — RED pre-fix (durable stuck at r1), GREEN post-fix. Anti-thrash guard: "a fenced tab does not re-claim when durable state already covers its delivery". | **FOUND → FIXED** | Fix: `LocalViewSession.deliver` re-opens the source and re-applies once when a superseded apply carries strictly newer coverage than the durable view (or any access transition). Two live tabs cannot ping-pong claims: equal coverage never re-claims. |
| TAB-02 | two tabs × concurrent commits | Concurrent complete-view commits from two writers interleave and corrupt membership (partial merge of two generations). | IDB rw txns serialize, and writer fence rejects the loser — but the loser's error path must not have already broadcast/published. | `commandMatrix.test.ts` "racing commits from two tabs settle on one coherent generation" — fenced loser rejected, exactly one generation of members/projections persists. | **ABSENT** (test) | |
| TAB-03 | two tabs × broadcast loss | BroadcastChannel messages are not delivered to bfcached/frozen tabs and have no delivery guarantee; a tab that misses commits renders stale durable state until something reconciles. | Correctness must not depend on broadcast (§12.3). `PrincipalLocalStateProvider` wires focus/visibilitychange → `reconcileLauncherGeneration` → `reconcileDurableHead`. Verify the full chain actually republishes affected views (affectedKeys=[] → republish-all path). | `convergenceMatrix.test.ts` "reconcileDurableHead republishes cross-tab commits without broadcasts" (channels null = every broadcast lost); provider wires focus+visibilitychange to reconcile. | **ABSENT** (test + wiring review) | bfcache caveat tracked separately in ACC-06. |
| TAB-04 | two tabs × commands (Step-2 readiness) | Two tabs drain the same command journal concurrently: double `send` of one command (server dedupes), then the loser's `settle` hits "Command receipt changed across replay" (`receivedAt` differs) → **IdentityError → reported as storage failure → healthy tab flips to degraded and dispatch closes**. | `valuesEqual(existing, receipt)` compares client-stamped `receivedAt`; concurrent drains are unsynchronized across tabs; commands.commit reports every non-fence error as storage failure. | `commandMatrix.test.ts` "concurrent two-tab drains of one command converge without corrupting the journal" — RED pre-fix: loser threw "Command receipt changed across replay" (client-stamped receivedAt differed) and reported it as a STORAGE FAILURE, flipping a healthy tab to degraded and closing dispatch. GREEN post-fix. | **FOUND → FIXED** | Fix: receipt replay-identity excludes the client-stamped receivedAt; the first stored receipt wins. Journal converges to one reconciled command + one receipt. |
| TAB-05 | two tabs × schema upgrade (deploy overlap) | New-bundle tab upgrades Dexie schema while an old-bundle tab holds the DB open → upgrade blocked (new tab hangs) or old tab's connection killed mid-write. | Dexie's `versionchange` default closes the old connection, but a mid-transaction close surfaces as DatabaseClosedError inside apply paths; also `blocked` leaves the new tab waiting forever if the old Dexie overrode versionchange. | `lifecycleMatrix.test.ts` "schema version overlap..." — the new bundle's open is NOT blocked (Dexie's default versionchange handler closes the old connection; proven under fake-indexeddb). Old-bundle loud failure rests on IDB VersionError semantics (not enforceable under fake-indexeddb), PLUS a new adapter-level guard: assertFence rejects any store whose metadata.schemaVersion is newer than the running bundle. | **ABSENT + HARDENED** | Defense-in-depth added for all FUTURE migrations: a stale bundle (service-worker cache) can never write records a newer schema migrated past, regardless of browser version-gate behavior. |
| TAB-06 | two tabs × different principals | Cross-principal broadcast contamination: tab on principal P1 receives P2's commit broadcast. | Channel name embeds databaseName (principal-scoped) AND receiveBroadcast re-checks principalKey — double guard; verify. | `convergenceMatrix.test.ts` "cross-principal broadcasts are ignored" — promiscuous shared bus; principalKey re-check holds even if channel names collided. | **ABSENT** (test) | |

## C. Command journal / optimistic state (Step-2 readiness unless noted)

| id | dimension combo | potential issue | steelman | experiment | verdict | notes |
|---|---|---|---|---|---|---|
| CMD-01 | crash mid-drain × sending | Crash after transport send, before settle: restart must treat `sending` as ambiguity → checking-receipt (server-deduplicated) and settle from getReceipt, never re-send blindly a non-replayable command. | drain() handles `sending` via markTransportUncertain; verify both replay policies including retryUntil expiry path. | `commandMatrix.test.ts` "restart drain resolves a crashed sending command via its receipt first" (fresh runtime over the same store; zero re-sends when the receipt exists) + commands.test.ts transport-ambiguity test (same-id replay for deduplicated, ambiguous-stop for non-replayable). | **ABSENT** (tests) | |
| CMD-02 | offline queue × app upgrade | Optimistic operations recorded under schema v1 replayed by app v2 that interprets them differently (design §11.2 requires concrete ops + operationSchemaVersion). | `operationSchemaVersion` exists on the record but nothing gates hydration/fold on it — a v2 reader may fold v1 ops wrongly. | Grep: `operationSchemaVersion` is persisted and immutability-checked but NOTHING gates folding/hydration on it — no version gate, no migration router exists. | **N/A — STEP-2 BLOCKER** | No product commands exist yet (write path is legacy). Before Track B3 write cutover: hydration must refuse to fold ops whose operationSchemaVersion is unknown and route them to explicit migration, per design §11.2. Recorded on the Step-2 checklist via this row. |
| CMD-03 | revocation × queued/sending command | Revoking the last grant must retire overlays, block dispatch, purge payload — and a sending command becomes ambiguous, not retried. | `revokeGrant` transitions statuses via direct puts (bypasses ALLOWED_COMMAND_TRANSITIONS — e.g. checking-receipt→ambiguous is not in the allowed table) — verify no path later chokes on the "illegal" state. | `commandMatrix.test.ts` "revocation retires queued and in-flight commands and nulls their payloads" — queued→blocked, sending/checking→ambiguous, awaiting→reconciled, payloads nulled, later drains dispatch nothing and do not throw. | **ABSENT** (test) | revokeGrant's direct status writes bypass ALLOWED_COMMAND_TRANSITIONS (checking-receipt→ambiguous is not in the table) but are internally consistent; drain handles every resulting state. |
| CMD-04 | ack-awaiting-coverage × view never advances | Command acknowledged with view-revision coverage but the covering view result never arrives (tab closed before subscription refire) → overlay pinned forever. | Overlay stays active until `commandHasDurableCoverage`; on next boot the view bootstrap should deliver a >= revision and reconcile — verify reconcileAcknowledgedCommands runs on any commit including the bootstrap replaceView. | commands.test.ts "receipt-before-view and view-before-receipt both retire optimism only after coverage" + canonical-write-set variant; adapter runs reconcileAcknowledgedCommands on EVERY commit (bootstrap replaceView included), so a restart before the covering refire reconciles on the next bootstrap. | **ABSENT** (existing tests + hand review) | |
| CMD-05 | rejection rollback over newer state | Rejected command rollback applies inverse patch over newer authoritative data (classic optimistic-UI bug). | Design mandates fold-removal not inverse patches; visibleView folds active ops over base. Verify a rejected command simply drops from the fold and newer base shows. | `commandMatrix.test.ts` "a rejection folds away over a newer authoritative base" — optimistic overlay wins while active; rejection reveals the newer server row untouched; no inverse patches exist anywhere in the fold. | **ABSENT** (test) | |
| CMD-06 | conflict-key ordering | Two commands same conflict key must drain in creation order even across restarts; independent keys concurrently — but `localSequence` assignment uses commit head; two commands queued in separate commits OK, verify enumeration order stable after hydration. | readCommands sorts by localSequence, createdAt, id — createdAt from client clock; clock skew between queueing tabs could reorder equal-sequence? localSequence unique per commit so total. | `commandMatrix.test.ts` "commands sharing a conflict key drain in durable creation order" — two runtime instances (tabs), order by durable localSequence. | **ABSENT** (test) | localSequence is the commit head — unique and total per store; createdAt is only a display tiebreaker. |
| CMD-07 | replay horizon | `retryUntil` passed (client clock) → replay-expired must be visible, non-dispatchable, and never auto-assigned a new ID. Client clock skew: a server retryUntil compared against a client clock 10min fast expires valid commands early. | receiptAbsent/expireReplayHorizon compare against Date.now(); server currently returns retryUntil:null (indefinite) so unreachable today, but the comparison is clock-skew-sensitive by design. | `commandMatrix.test.ts` "an expired replay horizon parks the command as replay-expired" — never dispatched, never renamed, stays parked on later drains. | **ABSENT** (test) | Clock-skew sensitivity noted: expiry compares server retryUntil to client Date.now(); server currently ships retryUntil:null so unreachable in prod. Flag when horizons ship. |

## D. Principal runtime / accounts

| id | dimension combo | potential issue | steelman | experiment | verdict | notes |
|---|---|---|---|---|---|---|
| ACC-01 | verify-upgrade path drops recovery hook | `verify()`'s offline-ready→server-verified fast path constructs a new engine **without `onStorageRecovered`** → after a transient storage failure, health stays degraded and dispatch stays closed until reload. | Read the code: installStore passes onStorageRecovered; verify()'s upgrade branch omitted it. | principalRuntime.test.ts "a commit after the verify-upgrade path restores degraded storage health" — RED pre-fix (stayed degraded), GREEN post-fix. | **FOUND → FIXED** | One-line fix: verify()'s upgrade-path engine now wires `onStorageRecovered`. |
| ACC-02 | account switch mid-flight applies | A→B switch while A's apply is in flight: A's commit must be fenced by principal epoch and adapter fence; nothing of A renders under B. | Covered by supersession fix 55d7de45 + tests; re-verify with an apply in flight across `lock()` and assert disk state of A untouched post-fence. | principalRuntimeSupersession.test.ts (55d7de45) + `lifecycleMatrix.test.ts` ACC-04 race test + `convergenceMatrix.test.ts` ENG-03: switch gates synchronously, in-flight applies rejected by principal epoch + adapter generation fence inside the txn. | **ABSENT** (existing + new tests) | |
| ACC-03 | two accounts same browser | Store bleed via shared grant-key strings ("bucket-catalog" identical across principals) if any code path resolves a store by grant key rather than database. | Grant keys are only meaningful within a principal DB; verify no global registry keys on grantKey alone (grep). | `lifecycleMatrix.test.ts` "the same grant string in two principal stores never interacts" — revocation in A leaves B intact; grants are rows inside a principal database, never a cross-store registry (grep-verified). | **ABSENT** (test + grep) | |
| ACC-04 | logout purge × in-flight commit | `lock({purge:true})` races an in-flight commit: purge deletes the DB while a txn is open → Dexie behavior; must not resurrect the DB after delete. | adapter.fence() is awaited before purge, and fence flips metadata so later commits reject — but a commit already past assertFence inside its txn could commit after fence? Same-txn serialization: fence txn (meta) vs commit txn (all tables) serialize; if commit txn started first it wins, then fence, then purge — acceptable (pre-logout write). Verify delete-after-open-txn doesn't throw unhandled. | `lifecycleMatrix.test.ts` "fence and purge racing an in-flight commit cannot corrupt or resurrect" — parked commit + concurrent fence: commit lands wholly-before or rejects wholly; post-fence commits reject; purge leaves nothing (fresh adapter reads null metadata). | **ABSENT** (test) | |
| ACC-05 | offline boot credential mismatch | Offline boot with a credential binding never verified on this device must fail closed (no protected paint). | resolveOffline → launcher.resolveOffline returns null → locked. Covered by launcher tests; hand-review + spot test. | launcher.test.ts + principalVerification.test.ts + live probe in this campaign: resolveOffline on a locked/unbound launcher returns locked "credential-not-previously-verified" (fail closed, no paint). | **ABSENT** (existing tests + probe) | |
| ACC-06 | frozen tab resumes after account switch elsewhere | bfcache-resumed tab on generation G renders/commits against a store fenced at G+1. | Every commit re-checks metadata.activeGeneration inside the txn; render-side: provider reconciles on focus/visibilitychange (but pageshow for bfcache-restored pages fires BEFORE focus in some flows; visibilitychange does fire). Verify listener coverage includes pageshow or that visibilitychange suffices on bfcache restore. | Every durable commit re-checks activeGeneration inside its txn (frozen tab cannot write, `lifecycleMatrix.test.ts` ACC-04); rendered-state reconcile wired to focus + visibilitychange + (added) pageshow — the canonical bfcache-restore signal. | **ABSENT + HARDENED** | pageshow listener added to PrincipalLocalStateProvider; bfcache restore without any of the three signals does not exist in practice. |

## E. Server contracts (Convex) — revision/coverage semantics

| id | dimension combo | potential issue | steelman | experiment | verdict | notes |
|---|---|---|---|---|---|---|
| SRV-01 | join drift × comments | **View revision only advances on comment-table writes, but the v2 projection embeds joined user fields (name, github_username, avatar).** `users.updateProfile` changes the projection with the revision UNCHANGED → client rejects "Equal server view revision carried divergent complete content" → durable v2 comments view frozen (and erroring on every refire) until some comment write advances the revision. Shadow: standing digest mismatch. Post-cutover: stale rendered names + a poisoned view. | updateProfile (users.ts:128) patches name/status with zero revision advances; projectComments embeds user.*; dexieAdapter threw on equal-coverage divergent content. | `writerHandoff.test.ts` "equal-revision content drift from the fenced live source updates the view" — RED pre-fix (PrincipalStoreFenceError), GREEN post-fix. Adapter contract test updated: equal-revision content converges to the latest fenced delivery; older revisions still rejected. | **FOUND → FIXED** | Root cause: the revision covers the comments-table writes, not the full query result (joins drift). Fix: at equal comparable coverage the latest FENCED delivery wins (writer + source-sequence fences already prove ordering); "moved backwards" and revision-order-integrity rejections unchanged. Access transitions at equal coverage remain rejected (security). |
| SRV-02 | access flip-flop × comments | Conversation made private then public again with no comment writes in between: forbidden clears the view (accepted — no-coverage transition), but the **re-granted result carries the same revision R** → "Equal server coverage cannot change authoritative access state" → view stuck non-granted until an unrelated comment write. | forbiddenView/missingView envelopes carry no coverage; privacy toggles didn't advance the comments head; writer.lastAccess="forbidden" + equal coverage → rejected; ALSO the forbidden transition invalidates the session's source, so even a NEWER re-grant was silently swallowed. | `writerHandoff.test.ts`: "a mounted session recovers from forbidden when re-granted at newer coverage" (RED pre-fix → GREEN) and "a re-grant at equal coverage stays blocked" (security pin, passes pre+post). Server: functions.guard.test.ts pins ≥6 `advanceCommentsAccessRevision` call sites. | **FOUND → FIXED** | Two-part fix: (1) client session re-opens its source for access transitions and newer-coverage grants; (2) server advances the comments head in setPrivacy / setTeamVisibility / setPrivacyBySessionId / reconfigureSession (when access inputs move) and the two internal share repairs. Residual documented: access regained via team-membership re-add alone still regrants at an unchanged revision → view stays cleared until the next comment write or privacy touch. Equal-coverage re-grant rejection is intentional (stale cached result must not resurrect revoked content). |
| SRV-03 | access loss while offline | Viewer loses access while offline; on reconnect the client must purge (forbidden) — verify the whole client path applies revocation and physically deletes rows+grants+projections. | forbidden transition path exists; verify end-to-end at adapter level incl. grant-count zero. | `convergenceMatrix.test.ts` "forbidden purges projections, membership, and grants atomically" + engine.test.ts canonical variant + revokeGrant code review (commands retired, payload nulled). | **ABSENT** (tests + hand review) | |
| SRV-04 | parent deleted | Conversation deleted → missingView (release, not revoke) — entities/projections of the view must still disappear from render, and cascade rules (are comments rows deleted server-side?) must not strand a regrantable state. | missing path releases grants and deletes the view; verify + confirm conversation deletion advances or the view can return via SRV-02-style equal-coverage block. | `commandMatrix.test.ts` "missing releases the view and a newer grant restores it" — release is not a security purge, writer retains lastAccess=missing, newer re-grant restores cleanly. Conversation-deletion cascade: viewers converge via missingView; orphaned comment rows are server hygiene only. | **ABSENT** (test + cascade audit) | Interacts with SRV-02 fix. |
| SRV-05 | multi-writer revision domain | Comments revision domain = conversation OWNER's principal; two viewers write concurrently → OCC on one local_view_heads row; verify serializability holds and no lost advance (Convex guarantees). Also: conversation ownership never changes today — if it ever does, the revision domain resets and coverage regresses ("moved backwards" → frozen view). | Revision read at query time from conversation.user_id; write path derives from the authoritative conversation row — consistent unless user_id mutates. | comments.v2.test.ts "merging principals migrates revision domains and command receipts" — RED without the fix (heads stayed under the merged-away principal; receipts stranded), GREEN with it. admin_mergeUser now migrates local_view_heads (collisions collapse to max+1, strictly newer than either domain) and local_command_receipts. | **FOUND → FIXED** | Real trigger: admin user-merge remaps conversations.user_id — the comments revision DOMAIN. Un-migrated heads reset viewers' coverage to 0 → moved-backwards rejection → frozen views; un-migrated receipts would re-execute replayed command ids post-merge (duplicate effects) once the write path is live. Ordinary product flows never mutate conversations.user_id (grep-verified). |
| SRV-06 | CLI/daemon writes | Daemon/CLI comment + bucket writes (cast comment, agent placeholder enrichment, cliCreateBucket etc.) must advance the same heads as web writes, or SRV-01-style freezes occur on those paths. | buckets cli* funcs share createBucketForUser (advances); comments agent paths at comments.ts:746/782/1002/1149 use runCommentViewTransition/patchCommentWithRevision — audit each writer incl. dispatch.ts:187 and users.ts cascade (functions.guard.test pins several). | Full write audit: inbox_buckets/bucket_assignments writers all advance the head (createBucketForUser, assignConversationToBucketForUser, cliRenameLabel, cliArchiveLabel, v2 handlers via runLocalCommand, dispatch applyPatches via bucketViewChanged). Comment writers pinned by functions.guard.test.ts (no raw inserts anywhere; dispatch/users/admin_mergeUser routed through revision-aware writers). | **ABSENT** (audit + CI guard) | Guard test greps named call sites AND asserts zero raw '.insert("comments"' repo-wide — structural, not just nominal. |
| SRV-07 | empty vs unauthenticated | v1 returned [] for unauthenticated; v2 must never let an unauthenticated/unavailable result erase durable state. | queryView passes unauthenticated through; contracts.apply returns null before touching engine; verify plus the useLocalView skip path. | `convergenceMatrix.test.ts` "an unauthenticated result leaves the durable offline view untouched"; contracts.apply returns null before the engine for unavailable/unauthenticated. | **ABSENT** (test + hand review) | |
| SRV-09 | access regained via team membership alone | A viewer whose access was revoked (view cleared, lastAccess=forbidden at revision R) and later restored purely by a team-membership re-add — with zero comment writes and zero conversation-field changes — is re-granted at the SAME revision R, which the client rejects by design (a cached result must not resurrect revoked content). Their comment view stays cleared until any head advance. | Membership changes affect `canAccessConversation` for every team conversation; advancing every such head on membership change is unbounded fan-out, so the server deliberately does not. | Documented residual of the SRV-02 fix (owner-reviewed 2026-07-30, accepted). Self-heals on the next comment write or privacy touch of that conversation; the durable state is empty-but-safe, never wrong. | ABSENT-BY-POLICY (accepted residual) | Revisit only if support reports surface it; a bounded fix would advance heads lazily on first forbidden→granted query transition observed server-side. |
| SHD-03 | post-cutover divergence monitoring | Once a slice is in cutover, v1 unsubscribes and digest evidence stops — and with the adapter's equal-revision tripwire deliberately retired (SRV-01), the shadow digests are now the ONLY standing divergence detector. If the validator is removed after the soak, silent divergence would have no detector at all. | Owner directive (2026-07-30): keep the shadow validator wired permanently. | IMPLEMENTED: useCutoverSpotCheck — 10% of cutover mounts re-subscribe v1 for one 10s digest window; sampled result never feeds the store (mode-gated callbacks). | **DONE (implemented + suites green)** | Owner-requested standing monitor. |
| SRV-08 | grant-key hygiene | `assertViewGrants` requires grant.scopeKey === viewKey; a future contract granting row-level keys from a DIFFERENT scope (e.g. shared entity across views) would be rejected at apply time — latent contract landmine, not user-facing today. | Current slices use one view-scoped grant; the engine writes scopeKey=viewKey for all row grants. | `commandMatrix.test.ts` "a view payload cannot borrow a grant bound to another scope" — cross-scope grant rejected with an identity error. | **ABSENT** (test) | Constraint stands for Track B4: a future contract sharing one grant across views needs a new adapter capability first. |

## F. Connectivity / timing

| id | dimension combo | potential issue | steelman | experiment | verdict | notes |
|---|---|---|---|---|---|---|
| NET-01 | cold-start offline | Boot with no network: offline-ready store paints durable rows; no spinner-forever; no accidental clearing when useQuery yields undefined indefinitely. | useLocalView requires offline-ready phase; deliver never called with undefined. Verify status semantics ("granted" from durable view). | localViewSession.test.ts "durable rows publish on open before any server delivery" + `convergenceMatrix.test.ts` unauthenticated retention test; useLocalView only delivers defined results. | **ABSENT** (tests + hand review) | |
| NET-02 | reconnect after long offline | Server moved many revisions; single fresh complete result must converge including deletions (no tombstones needed for projection views). | replaceView is total for the view; covered by live demo 2026-07-29; add deterministic regression. | `convergenceMatrix.test.ts` "a single post-reconnect complete result converges membership exactly" (rev5→rev40 with deletions and edits); live cold-start deletion demo 2026-07-29. | **ABSENT** (test + live evidence) | |
| NET-03 | flaky reorder across sources | Two different views' results interleave arbitrarily — no cross-view interference (separate keys). | Keys namespaced; commit txns serialized. | `convergenceMatrix.test.ts` "interleaved applies to two views stay independent". | **ABSENT** (test) | |
| NET-04 | subscription silent death (sleep/wake) | Laptop sleeps; websocket dies silently; on wake Convex client reconnects and refires — until then durable state renders. No client-side staleness detection needed for correctness, but verify wake doesn't produce a stale-epoch apply. | Convex client owns reconnect; source epoch unchanged (no remount) → same fence still valid → applies fine. Only risk is TAB-01 (writer died elsewhere). | Hand review post-fix: wake keeps the same source epoch and writer fence (no remount), so refired results apply normally; if another tab advanced the durable view meanwhile, the writer-handoff rule (newer coverage) re-claims; if the store moved past this tab, equal coverage no-ops. Randomized interleavings (NET-05) cover the schedules. | **ABSENT** (review + NET-05 property test) | |
| NET-05 | racing tabs during reconnect | Both tabs' subscriptions refire simultaneously on reconnect → both try to claim/commit → thrash bounded, converges (see TAB-01 fix). | Post-fix property: N tabs, arbitrary delivery interleaving, final durable content equals last server result. | `convergenceMatrix.test.ts` "randomized two-tab interleavings converge to the newest delivered revision" — 12 deterministic seeds, mid-stream writer-tab closes. | **ABSENT** (post-TAB-01-fix property test) | |

## G. Lifecycle / storage

| id | dimension combo | potential issue | steelman | experiment | verdict | notes |
|---|---|---|---|---|---|---|
| LIF-01 | IDB eviction between sessions | Browser evicts the principal DB (best-effort storage); next boot: offline → must fail closed to locked (no ghost paint), online → clean re-bootstrap, and **no code path treats missing store as "new user with empty data" while verified**. | openOffline throws FenceError("not initialized") → locked; verify() recreates via activateVerified with fresh head=1 — but LAUNCHER still has binding; check verify() on an evicted DB rebuilds without identity confusion. | `lifecycleMatrix.test.ts` "an evicted store fails closed offline and rebuilds on verification" — openOffline rejects (no ghost paint), activateVerified rebuilds empty with fresh head; runtime-level exists()-check covered by launcher/principalRuntime suites. | **ABSENT** (test) | navigator.storage.persist() is still not requested anywhere — optional durability hardening, noted for the checklist. |
| LIF-02 | quota exhaustion mid-commit | QuotaExceededError inside the rw txn → txn aborts → onStorageFailure → degraded (dispatch closed), UI keeps last durable state; recovery bounded (3) — but does a quota failure during `claimViewWriter` leave the session dead with no retry (status loading forever)? | beginSource failure → report("open") → console only; session queue resolves null; all delivers no-op. View shows EMPTY loading forever on a transient failure. | `lifecycleMatrix.test.ts` "a session whose initial open failed recovers on a later delivery" — RED pre-fix (view dead until remount after one transient claim fault), GREEN post-fix. | **FOUND → FIXED** | Fix: LocalViewSession.deliver re-attempts the source open when the initial open failed; each delivery is a fresh chance. Quota-exhaustion path unchanged: commit failures degrade health, bounded recovery (3) restores it. |
| LIF-03 | crash atomicity | Crash at each fault point (after-operations, after-head-write) must leave either full commit or nothing; hydration reads last complete state. | Single IDB txn covers ops+head; fault injection throwing aborts whole txn — verify BOTH fault points roll back everything (head never advances without ops and vice versa). | `convergenceMatrix.test.ts` "a crash inside the writer claim rolls back atomically" + engine.test.ts "a storage rollback never publishes an in-memory commit" (both fault points inside one IDB txn). | **ABSENT** (tests) | Likely covered by existing adapter tests — verify & extend for claimViewWriter. |
| LIF-04 | reload mid-apply | Reload between durable commit and publish loses nothing (publish is derived); hydration must republish from durable state incl. viewWriter lastAccess for non-granted views. | LocalViewSession initial republish reads snapshot; status falls back to writer.lastAccess. | `convergenceMatrix.test.ts` "a fresh session over a revoked store publishes forbidden with zero rows" + localViewSession.test.ts durable-first paint. | **ABSENT** (tests) | |
| LIF-05 | private browsing / IDB unavailable | IDB open fails entirely → failClosed("...") not a crash loop; app remains usable on legacy path. | resolveOffline/verify catch → failClosed; verify UI copes (out of unit scope: note). | `lifecycleMatrix.test.ts` "verify fails closed when the principal store cannot open" — RED pre-fix: stores.open was awaited OUTSIDE verify()'s try, so an IndexedDB open failure (private browsing, corrupted profile) stranded the runtime in "opening" with the protected gate waiting forever. GREEN post-fix (failClosed + rethrow). | **FOUND → FIXED** | resolveOffline already handled this; verify() did not. |
| LIF-06 | service-worker-stale bundle | Old bundle (old contract ids/schema) writes to a DB the new bundle already upgraded — schema v guard + contract identity checks must reject rather than corrupt. | viewWriter contract mismatch → IdentityError; Dexie schema version mismatch → old code's `version(3)` absent → Dexie throws VersionError on open (older code opening newer DB). Surfaces as storage failure → degraded. Acceptable-fail-closed; verify not silent. | Covered by TAB-05's schema-fence test + hand review: contract identity checks (viewWriter/entity/tombstone owner mismatch -> IdentityError) reject cross-contract writes; the new schemaVersion fence rejects newer-store writes. | **ABSENT** (test + review) | |
| LIF-07 | conversationMessages/legacy tables | Legacy compat tables inside the principal DB can't bypass fences (writes only through commit ops). | All ops fence-checked in commit. Hand review. | Grep audit: legacy compat tables (legacyCollections/legacyMeta/legacyOutbox/conversationMessages) are written ONLY through adapter.commit store operations — every path fence-checked; reads require the fence too. | **ABSENT** (audit) | |

## H. View shapes / objects

| id | dimension combo | potential issue | steelman | experiment | verdict | notes |
|---|---|---|---|---|---|---|
| VIEW-01 | complete + segment coexistence | A view with durable segments receiving a complete replacement at EQUAL coverage throws (adapter guard) — legit for current slices (no segments) but verify a complete replacement at NEWER coverage cleanly deletes segments. | replace-complete-view deleteView removes segments; equal-coverage + segments → throw path exists — confirm unreachable for slice contracts. | `lifecycleMatrix.test.ts` "complete replacement supersedes segments only with newer coverage" — segments deleted at newer coverage; fenced at equal coverage (pinned). | **ABSENT** (test) | |
| VIEW-02 | store-fed feeds vs full-object render (cutover parity) | In cutover mode the v2 complete view fed the store through `syncTable` with the registry's `isDelta: true` (upsert-only) semantics — so a teammate's DELETION, correctly converged in the durable v2 view, never pruned from the rendered store or its IDB persistence. The headline v2 win (deletion convergence) was silently negated at the last hop, and shadow validation could not see it (it compares the raw v1 query vs the v2 view, not the store). | comments/buckets/bucketAssignments are all `isDelta: true` in SYNC_REGISTRY; the cutover effects passed no opts; readers read the store. | `store/__tests__/cutoverFeedPrune.test.ts`: isDelta alone retains a server-deleted row (proves prune mandatory); `pruneAbsentScope` removes in-scope absent rows with a durable exclude, spares other conversations, and spares rows with pending optimistic state. Hooks now pass a scoped prune. | **FOUND → FIXED** | Step-1 cutover gate blocker, caught before the flip. Comments prune scoped to the conversation; buckets/assignments prune the whole personal catalog (view is complete for it). |
| VIEW-03 | buckets kind-tagged rows | bucketsPrincipalView splits kinds by prefix; digest uses `.row` — a bucket and an assignment with the same _id prefix collision (`bucket:x` vs `assignment:x`) impossible; verify entityKey uniqueness guard (digest throws on dup). | Convex ids unique per table but cross-table collision of same id string effectively impossible; dup key throws in digest — noisy but visible. | shadowDigest.test.ts "rejects duplicate identities" (digest throws on dup keys — divergence surfaces loudly); useLocalView rows are Map-keyed so dup entityKeys collapse before render. | **ABSENT** (existing test + review) | |
| VIEW-04 | windowed/paged legacy regression | Unmigrated collections (tasks, docs, inbox) must be untouched by v2 flags — no v2 write path can affect their store slices. | Flags gate hooks; grep for accidental coupling. | featureFlags.test.ts (fail-closed double gate: global rail + per-slice mode, typo->off) + hook review: useLocalView(enabled:false) skips the query and never constructs a session; cutover feed effects are mode-gated. Flag-off build is bytewise v1 behavior. | **ABSENT** (tests + review) | |
| VIEW-05 | demand view churn | Rapidly switching conversations mounts/unmounts many sessions; writer epochs grow monotonically per view; no unbounded storage growth per churn (one viewWriter row per view, head bumps only). | claimViewWriter puts one row per key; commits add head only. | `convergenceMatrix.test.ts` "open/close churn keeps one writer row and bounded content" — 25 cycles, table sizes constant. | **ABSENT** (test) | Writer epoch grows monotonically (a number), never row count. |

## I. Shadow validation / cutover instrumentation

| id | dimension combo | potential issue | steelman | experiment | verdict | notes |
|---|---|---|---|---|---|---|
| SHD-01 | false-equal | Digest treats undefined-valued keys as absent (stableJson filter) — v1 row {x: undefined} vs v2 row without x digest-equal: fine. But **false-negative risk**: v1 sorts rows differently than v2 → digest sorts by key first — order-insensitive. Verify no field the UI renders is outside the digest (user.image explicitly excluded — documented). | comparableComment strips image deliberately; anything else additive would hide divergence from the gate. | Field-parity audit: v1 getConversationCommentSummary rows = full comment doc + user{_id,name,github_username,github_avatar_url}; v2 projectComments = same + user.image. comparableComment normalizes user to exactly the v1 field set ON BOTH SIDES and keeps the full doc; only the additive user.image is outside the gate (documented). Digest sorts by key -> order-insensitive. | **ABSENT** (audit) | |
| SHD-02 | quiescence race | 600ms debounce records a comparison from a half-settled pair (v1 updated, v2 mid-apply) as mismatch noise — or worse, timer fires with stale closure values producing phantom mismatch counts that block the cutover gate. | Effect re-arms on any input change; a mismatch only records if inputs held 600ms — genuine settled divergence. Phantom risk low; verify with SRV-01 scenario (standing genuine mismatch is the DESIGNED signal). | shadowValidation.test.ts + review: comparison records only after both feeds hold still 600ms (any input change re-arms the timer); a standing mismatch is the designed signal, not noise (SRV-01 proved the detector works). | **ABSENT** (existing tests + review) | |

## J. Performance / scale (budget rows, not bug rows)

| id | dimension combo | potential issue | steelman | experiment | verdict | notes |
|---|---|---|---|---|---|---|
| PRF-01 | readSnapshot full-scan per commit | Every commit republishes every open session via full-table readSnapshot (all entities, all commands, all projections) — O(store) per commit per session; fine for two slices, but a busy store (messages later) makes commits quadratic. | affectsThisView matches broad key classes (any entity:, any command:); every session re-reads everything. | Probe (fake-indexeddb): 5k projection rows -> readSnapshot p50 22ms / p95 25ms per republish; first commit 235ms. Fixture numbers, not browser numbers — real-IDB spot-check belongs to the §18.6 budget pass before any large collection migrates. | **MEASURED** (budget evidence) | Full-scan-per-commit-per-session is fine at slice sizes; revisit before messages/tasks-scale views. |
| PRF-02 | broadcast storm | N tabs × M commits → N×M reconciles each doing readHead; bounded, but claimViewWriter ALSO broadcasts (head bump on every mount). | Churn from VIEW-05 amplified across tabs. | Bounded by design: broadcasts carry only head+keys; reconcile reads head once and snapshots only when behind. Claim-on-mount broadcast churn measured trivially small at slice scale (VIEW-05 churn test). | **MEASURED/BOUNDED** | |
| PRF-03 | complete-view refire × large collections | Every granted refire rewrites the ENTIRE view (delete-N + insert-N rows) even when 99% identical — write amplification per Convex refire. | Scaling probe: recommit of an existing view grew 4× per size doubling under fake-indexeddb (227ms@500 → 15.4s@4000). The curve is a fixture artifact (array-splice deletes; real IDB is O(N log N)) but the full-rewrite design fact is real. | Recorded probe numbers; real-browser measurement required by the §18.6 budget gate before any large collection adopts complete-view replacement. | **MEASURED — DESIGN NOTE** | Current slices are small (comments 10s–100s, buckets 10s): fine. Messages/tasks must use windowed segments or deltas, exactly as the design already mandates. |

---

## R. Research-taxonomy sweep (literature + practitioner postmortems)

Sources: a 130-item external sweep (background research agent: Linear/Figma/Replicache/Zero/PowerSync/ElectricSQL/Couch/Firestore postmortems and design docs, WebKit/Chromium bug trackers, Dexie references, Convex OCC/pagination docs, MDN storage/lifecycle) diffed item-by-item against this matrix, plus direct spot searches. Every one of the 130 items maps to a row here, a platform guarantee, or an out-of-layer owner; the deltas that survived the diff are R-10..R-13. The Step-2 gate now reads: CMD-02 (fold gate) + R-01 (storage.persist) + R-11 (dependent-command authoring rule).

| id | dimension combo | potential issue | steelman | experiment | verdict | notes |
|---|---|---|---|---|---|---|
| R-01 | Safari/WebKit × 7-day ITP eviction × pending commands | Safari deletes ALL script-writable storage (IndexedDB included) after 7 days of no user interaction. Read model: fine (cache, re-bootstraps — LIF-01). Command journal: **durable offline intent is destroyed wholesale**, violating "no durable command is silently abandoned" — and the user is never told. | Documented WebKit behavior since 13.1; nothing in the codebase calls `navigator.storage.persist()`; a Step-2 user who queues a comment offline and reopens the PWA 8 days later has silently lost it. | Not reproducible in unit rig (platform policy). Behavior on eviction IS tested (LIF-01: fail-closed, clean re-bootstrap). The exposure is the journal, which does not exist in prod yet. | **N/A today — STEP-2 BLOCKER** | Before write cutover: request `navigator.storage.persist()` at first verified boot, surface the grant state in the sync inspector, and treat denied-persistence as reduced durability capability (design §11.2 degraded-capability rule). Filed alongside CMD-02. |
| R-02 | IDB corruption / partial open failure | Chrome profile corruption, disk errors, or WebKit bugs can make `open()` fail or a DB open with missing object stores — historically a top practitioner pitfall. | Corruption is indistinguishable from eviction at open time; a half-usable DB is worse than a missing one. | Open-failure paths: verify() and resolveOffline both fail closed (LIF-05 test; resolveOffline pre-existing). Missing-store shape: Dexie's schema declaration recreates stores on version open; a same-version DB missing tables would surface as op-level errors → onStorageFailure → degraded (LIF-02/quota family). | **ABSENT** (fail-closed paths tested; degraded path for op-level failures) | Recovery from persistent corruption = logout/purge + re-verify, which rebuilds from authoritative queries by design. |
| R-03 | preview/prod deployments × one browser origin | Two deployments sharing an origin would share IndexedDB namespaces → cross-deployment store bleed (rows from staging rendered in prod). | Classic local-first pitfall when DB names are constant. | Verified: launcher and principal DB names embed `AUTH_STORAGE_NAMESPACE` = sanitized CONVEX_URL (lib/localAuth.ts:21), so different backends get disjoint launcher + store namespaces on the same origin. | **ABSENT** (wiring verified) | |
| R-04 | Dexie transaction auto-commit on foreign await | Dexie transactions auto-commit if you await a non-IDB promise inside them — a notorious silent-corruption source: half the operations land, the rest run outside the transaction. | The adapter's commit path is a big rw transaction full of awaits; one foreign await (crypto, fetch, timer) would break atomicity invisibly. | Audit of every await inside `db.transaction` scopes in dexieAdapter.ts: all are Dexie table ops or pure-sync helpers; SHA-256 hashing lives server-side (Convex) and in shadowDigest (outside transactions); the only foreign awaits are the TEST-ONLY fault injectors. Crash-atomicity tests (LIF-03) double-check the boundary empirically. | **ABSENT** (audit + atomicity tests) | Keep the rule in review: no fetch/crypto/timer awaits inside adapter transactions. |
| R-05 | retry storms / dead letters | Offline-first postmortems: unbounded retry loops hammer the server on reconnect; failed items with no terminal state become invisible dead letters. | A drain loop with per-command retries and no backoff could storm; commands with no terminal state linger forever. | Drain design review + CMD tests: one pass per drain invocation (no internal retry loop); per-conflict-key sequential; terminal states are closed (reconciled/rejected/replay-expired/blocked) and CMD-03/07 prove parking; transport-level backoff is the Convex client's. Dead letters are visible by design (`replay-expired`, `ambiguous` are user-surfaced states, not queue residue). | **ABSENT** (design review + CMD tests) | Step-2 UI must actually render the parked states — noted on the Step-2 checklist via CMD-02/R-01 lines. |
| R-06 | leader election alternatives (Web Locks, heartbeats) | Literature default for multi-tab writers is leader election (Web Locks API / heartbeat leases); naive leases die with the leader and freeze followers — exactly the failure class TAB-01 found. | Our design deliberately avoids leases: durable writer epochs + claim-on-need. | TAB-01 fix implements handoff-on-newer-coverage: no heartbeat, no lease expiry, correctness from the epoch fence (design §12.3 — "correctness comes from the epoch comparison, not the timer"). Randomized property test (NET-05). | **ABSENT** (fixed + property-tested) | Web Locks could later reduce claim churn as a pure optimization; never a correctness dependency. |
| R-07 | mobile quota prompts / tiny quotas | Older/mobile browsers cap IDB (historically 5–50MB with prompts); a large store hits QuotaExceededError mid-transaction. | Quota failure mid-commit could half-apply. | Txn atomicity on abort (LIF-03); quota failure surfaces as storage failure → degraded → bounded recovery (principalRuntime tests). Slice data volume is KBs today; PRF rows gate large collections. | **ABSENT** (tests + budget gate) | |
| R-08 | exactly-once delivery impossibility | The literature's core theorem: exactly-once between processes is impossible; systems must pick at-least-once + dedupe. A client that assumes exactly-once double-applies on ambiguous transport outcomes. | Any send path without server dedupe duplicates on retry-after-timeout. | Server: `runLocalCommand` dedupes by (principal, commandId) with an argument fingerprint (COMMAND_ID_REUSED on divergent intent — server tests); comment client_id dedupe for the legacy path. Client: `checking-receipt` protocol replays the SAME id only within the horizon (commands.test.ts, CMD-01/07); non-replayable effects park as `ambiguous`, never silently re-sent. | **ABSENT** (server + client tests) | Matches design §11.3/§15.6's honest guarantee. |
| R-09 | clock classes (skew, NTP jumps, backwards time) | Wall-clock used as ordering breaks on skew/backwards jumps (documented repeatedly in sync postmortems). | Any Date.now() in an ordering role is a landmine. | Audit: every ordering domain is logical (server revisions, writer epochs, source sequences, local commit heads). Date.now() appears only in telemetry/display fields (grantedAt, createdAt, updatedAt) and the CMD-07 replay-horizon comparison (flagged there; server currently ships null horizons). Design invariant 13 enforced. | **ABSENT** (audit) | |
| R-10 | iOS/WebKit × cold start × storage open hang | `indexedDB.open()` can hang FOREVER with no success/error/blocked event (WebKit #226547: first open after iOS cold start; Chromium #242115: after a stuck versionchange txn). An unbounded await stranded the runtime in "opening" — the hang variant of LIF-05's thrown-open failure, unreachable by its fix. | Documented, still-reproducible browser bugs; every factory open awaited unboundedly. | `lifecycleMatrix.test.ts` "a hung storage open times out into the fail-closed path" — boundStorageOpen races a 15s deadline; timeout throws into the existing fail-closed handling (verify/resolveOffline both covered). | **FOUND (latent, browser-bug-triggered) → FIXED** | Fix commit 4a9ff5f1. exists() checks bounded too (they also open the DB). |
| R-11 | dependent commands × temp ids (Step-2 design rule) | A queued command whose args reference a PARENT command's server-assigned id (assign-to-just-created-bucket) cannot be drained until the parent's receipt supplies the real id; conflict-key ordering serializes dispatch but does NOT rewrite args. Pushing a temp id produces a rejection; auto-minting a new logical command on retry double-applies. | Taxonomy #95 (Linear/TanStack DB temp-id reconciliation); our journal persists concrete args at queue time. | Design-level: no such command pair exists yet (bucket create returns the id to the CALLER, which queues the dependent after the receipt). Rule recorded for Track B3 command-contract authoring: dependents either queue after the parent receipt or declare an explicit arg-rewrite binding. | **N/A today — STEP-2 AUTHORING RULE** | Filed with CMD-02/R-01 on the Step-2 gate. |
| R-12 | hot view head × concurrent writers (OCC) | `local_view_heads` serializes every covered write to one row per (principal, contract, view) — a deliberately introduced contention point. A conversation with many simultaneous commenters (or a future wide-fan-out view) contends on one Convex document; OCC retries are deterministic and safe but throughput-bounded, and a fleet-reconnect drain amplifies it (taxonomy #83/#115). | Convex OCC retry amplification on hot documents is the platform's documented scaling hazard. | Review: current slices bound contention to human comment rates on one conversation (fine). Constraint recorded: a Track B4 collection with machine-rate concurrent writers must shard its head or use append-style coverage. | **ABSENT at slice scale — DESIGN CONSTRAINT** | |
| R-13 | platform-covered & adjacent classes (condensed) | Taxonomy items resolved by platform or adjacent layers, recorded so the coverage claim is explicit: captive-portal/garbage payloads (Convex websocket protocol + typed envelope decode reject; never applied raw), navigator.onLine (unused anywhere in the layer), subscription gaps (Convex subs re-deliver full current results on reconnect — stateful queries, not event streams), HTTP retry/reorder (Convex client session protocol), auth refresh races + JWT clock skew (auth library layer — flagged to app owners, outside this layer), BroadcastChannel blocks bfcache (accepted perf cost; reconcile-on-pageshow already handles restores), cross-view snapshot tearing (per-view equality is the declared contract — composite Track B4 views must use one query per consistent view), pinned-cursor pagination (mandatory for future windowed contracts), Figma-class entity semantics (soft-delete/reparent/undo — Track B4 canonical-collection decisions). | Taxonomy #51, #74–78, #81, #84/86/90, #104–107, #117–118. | Cross-referenced review; no code change warranted in this layer today. | **ABSENT / OUT-OF-LAYER (documented)** | Structural confirmations from the sweep: overlay-on-authoritative-base + acknowledged-awaiting-coverage is exactly the answer to rollback-clobber, write-then-read revert flash, and never-advance-base-past-unacked-write (taxonomy #91/#96/#121/#122); receipts = Replicache lastMutationID (#108); complete views = always-full-resync, so no delta-window/falling-behind class exists (#102). |


---

## Fix decisions taken (flagged for owner review before push)

1. **SRV-01/SRV-02 family — the view revision covers the view's own table writes, not the full query result.** Decision taken: (c) + server-side advances. Client: at equal comparable coverage, granted→granted content converges to the latest **fenced** delivery (the writer + source-sequence fences already prove it is the newest payload this store has observed); "moved backwards", revision-order-integrity, and access-changes-at-equal-coverage rejections are unchanged. Server: every access-changing conversation mutation advances the comments head (guard-test-pinned). Deliberately **weakened**: the "equal revision carried divergent content" tripwire — it fired on legitimate join drift (user rename) and froze views; shadow digests + telemetry remain the divergence detectors. Residual: access regained purely via team-membership re-add regrants at an unchanged revision and stays cleared until the next head advance (narrow, self-healing, security-conservative).
2. **TAB-01 — writer handoff.** Implemented in `LocalViewSession.deliver`: one re-open + re-apply when a superseded delivery is strictly newer than the durable view (or is an access transition). Equal coverage never re-claims, so two live tabs cannot ping-pong the writer.
