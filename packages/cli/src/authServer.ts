// The localhost callback listener `cast auth` opens for the browser to POST
// credentials back to. Lives in @platform/auth/cli, extracted from here.
export { AuthServer } from "@platform/auth/cli";
export type { AuthResult, AuthServerOptions } from "@platform/auth/cli";
