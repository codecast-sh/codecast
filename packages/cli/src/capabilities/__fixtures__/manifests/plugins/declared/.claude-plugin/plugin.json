{
  "name": "declared",
  "description": "Synthetic: skills path ADDS to the default scan, commands path REPLACES it.",
  "skills": ["./extra/added-skill"],
  "commands": ["./custom-commands"],
  "userConfig": {
    "apiKey": { "type": "string", "title": "API key", "sensitive": true, "required": true },
    "workdir": { "type": "directory", "title": "Working directory" }
  }
}
