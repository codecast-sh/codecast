// The extractor and FileChange type now live in @codecast/convex/convex/fileChanges
// so the exact same logic materializes changes server-side at message ingest.
// Re-exported here to preserve existing web import paths.
export * from "@codecast/convex/convex/fileChanges/extractor";
